# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
# HTTP и WebSocket (/ws) слушают один порт (SERVER_PORT). WEBSOCKET_PORT в .env опционален.
# WEBSOCKET_PORT=8080

# Публичный адрес для ответа OTA (ESP не может подключиться к ws://0.0.0.0/...).
# Укажите IP или DNS сервера, видимый с устройства:
# PUBLIC_HOST=90.156.254.46
# Или полный URL без завершающего слэша (имеет приоритет):
# WEBSOCKET_PUBLIC_URL=ws://90.156.254.46:8080

# Database Configuration
# SQLite example:
# DATABASE_URL=sqlite:mr_robot_console.db
# PostgreSQL example:
# DATABASE_URL=postgresql://user:password@localhost:5432/mr_robot_console
# MySQL example:
# DATABASE_URL=mysql://user:password@localhost:3306/mr_robot_console

# LLM Configuration
# LLM_PROVIDER=grok|openai
LLM_PROVIDER=grok

# Grok (xAI) Configuration
GROK_API_KEY=your-api-key-here
GROK_API_URL=https://api.x.ai/v1
GROK_MODEL=grok-beta
GROK_MAX_TOKENS=2048
GROK_TEMPERATURE=0.7

# OpenAI LLM Configuration (used when LLM_PROVIDER=openai)
# OPENAI_LLM_API_KEY=your-openai-api-key
# OPENAI_LLM_API_URL=https://api.openai.com/v1
# OPENAI_LLM_MODEL=gpt-4o-mini
# OPENAI_LLM_MAX_TOKENS=2048
# OPENAI_LLM_TEMPERATURE=0.7

# STT (Speech-to-Text) Configuration
STT_PROVIDER=whisper
# STT_API_URL=https://api.openai.com/v1/audio/transcriptions
# STT_API_KEY=your-stt-api-key

# TTS (Text-to-Speech) Configuration
TTS_PROVIDER=openai
# TTS_API_URL=https://api.openai.com/v1/audio/speech
# TTS_API_KEY=your-tts-api-key
TTS_VOICE=alloy

# Storage Configuration
STORAGE_BASE_PATH=./storage
# STORAGE_FIRMWARE_PATH=./storage/firmware
# STORAGE_ASSETS_PATH=./storage/assets
# STORAGE_UPLOADS_PATH=./storage/uploads

# Security Configuration
JWT_SECRET=change-me-to-secure-random-string
HMAC_KEY=change-me-to-secure-random-string

# MQTT Configuration (optional, requires mqtt feature)
# MQTT_ENABLED=false
# MQTT_BROKER=localhost:1883
# MQTT_CLIENT_ID=mediarise-robot-console
